management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: always
logging:
  level:
    com:
      alibaba:
        cloud:
          client8081: debug



#feign:
#  sentinel:
spring:
  boot:
    admin:
      client:
        url: http://127.0.0.1:6688
#    enabled: true