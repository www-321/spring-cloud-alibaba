spring:
  application:
    name: provider

  cloud:
    nacos:
      discovery:
        enabled: true
        server-addr: 192.168.20.130:8848
      config:
        file-extension: yml
        server-addr: ${spring.cloud.nacos.discovery.server-addr}
        prefix: provider1
